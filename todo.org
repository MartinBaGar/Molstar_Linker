* Inbox
** DONE Context menu incompatible with github and gitlab
CLOSED: [2026-06-24 Wed 20:16]
Currently, url is passed as is and cannot be read by molstar as a transformation is needed for github and gitlab (it work on fishare for example).

[[file:~/git-repos/molstar_linker/src/background.ts::// NOTE: need url transformation for github and gitlab]]
** TODO Enhance loading div handling
Currently, the loading div is displayed by default even when there is nothing to load (empty viewer). I am not even sure it it even needed in any cases. I have to inspect that.

[[file:~/git-repos/molstar_linker/src/viewer.ts::function spawnIframe(]]
** DONE Context-Menu upgrade
CLOSED: [2026-06-24 Wed 23:19]
Currently, right-clicking a link in a web page summons the format selector everytime. It should do so only if none of the allowed are clearly indentifiable in the url
** TODO Need convenience rewrite
Currently it opens the extension options page in a new tab and proposes to register the domain there by click. Instead it could simply update the extension settings, then update the popup with visual confirmation and update any existing options tabs with the new domain.

[[file:~/git-repos/molstar_linker/src/popup.ts::enableBtn.addEventListener('click', () => {]]
** TODO Create an utils directory to gather shared elements
*** DONE Unify url safety check function
CLOSED: [2026-06-24 Wed 23:19]

[[file:~/git-repos/molstar_linker/src/viewer.ts::if (!isSafeUrl(rawUrl)) {]]
[[file:~/git-repos/molstar_linker/src/background.ts::function isSafeUrl(urlStr: string): boolean {]]
*** DONE Unify default domain check
CLOSED: [2026-06-19 Fri 13:06]

- [[file:~/git-repos/molstar_linker/src/viewer.ts::const isDefault = DEFAULT_DOMAINS.some(d => targetDomain.includes(d));]]
- [[file:~/git-repos/molstar_linker/src/popup.ts::const DEFAULT_DOMAINS = \['github.com', 'raw.githubusercontent.com', 'gitlab.com', 'rcsb.org', 'alphafold.ebi.ac.uk'\];]]
*** TODO Update default domains

[[file:~/git-repos/molstar_linker/src/viewer.ts::// SCENARIO 3: URL present but format unknown (context-menu path)]]
** TODO Annoying page reload
When accessing a page, it first displays the badges then seems to perform a reload making the badges disappear and reappear. Very annoying.
** TODO Review each file before release
The following scripts were the only existing in =./src/= at the moment of this file writing (<2026-06-19 Fri>). Anything else present in =./src/= were added later on.
- [ ] background.ts
- [ ] config.ts
- [ ] content.ts
- [ ] extensions.ts
- [ ] native-builder.ts
- [ ] options.ts
- [ ] permissions.ts
- [X] popup.ts
  Added proposition of domain authorization rewrite
- [ ] sandbox.ts
- [ ] types.ts
- [ ] viewer.ts
** TODO Ensure Firefox compatibiliy
- Rewrite was done with testing on Chrome only, checks must be done with Firefox.
  Was partly done to register scripts but might have to be consolidated
- browser api must be changed to general instead of using =chrome=.
** TODO Add unit tests
